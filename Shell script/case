echo "Hey, choose an option:"
echo "a = See the current date"
echo "b = List all files in the current directory"

read choice

case $choice in
  a)
    date
    ;;
  b)
    ls
    ;;
  *)
    echo "Not a valid input"
    ;;
esac
